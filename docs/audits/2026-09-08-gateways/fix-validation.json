{
  "date": "2026-09-09",
  "baselineRevision": "1ee2fd7cddda98ead161baf40a91526b13683fa4",
  "gatewayCommand": "bun run test:gateways",
  "gateways": {
    "pass": 1337,
    "skip": 0,
    "fail": 0,
    "files": 41
  },
  "workspaceCommand": "bun run test",
  "originalWorkspace": {
    "pass": 2547,
    "skip": 1170,
    "fail": 91
  },
  "currentWorkspace": {
    "pass": 3410,
    "skip": 334,
    "fail": 78
  },
  "newFailingCases": [],
  "remainingPreexistingFailingCases": [
    "AC6: cross-gateway outcome consistency on create shapes > stripe: success:false pending is not a failed decline \u2192 outcome=indeterminate, paid=false",
    "AC7: RefundOperationResult parallel mapping > maps completed refund to succeeded",
    "AC7: RefundOperationResult parallel mapping > maps indeterminate with reconciliationRequired: true literally",
    "AC7: RefundOperationResult parallel mapping > maps pending refund without treating as terminal failure",
    "AC7: RefundOperationResult parallel mapping > uses dual-written outcome when present (map prefers explicit outcome)",
    "AC8: Money/AmountInput regression (no float money math) > AmountInput accepts number | Money via normalizeAmountInput",
    "CreateCheckoutSessionParamsSchema cancelUrl (P22R3-CANCEL-URL) > accepts http(s) cancelUrl for payment and subscription",
    "CreateCheckoutSessionParamsSchema cancelUrl (P22R3-CANCEL-URL) > requires cancelUrl for explicit mode payment",
    "CreateCheckoutSessionParamsSchema cancelUrl (P22R3-CANCEL-URL) > requires cancelUrl for payment mode (default)",
    "P610-INF: infer fail-closed money / no dual-write lie > success:false + pending/processing/approved infers indeterminate",
    "idempotencyKey validation (CORE-2) > accepts non-empty keys including those with internal spaces",
    "mock harness stands in for offline full suite > mock configured like stripe caps passes full suite",
    "mockGateway > FakeClock applyLatency re-checks abort after advance (P05-TK-1)",
    "mockGateway > FakeClock applyLatency yields so a pre-queued abort is observed (P05-TK-1)",
    "mockGateway > aborts long latency create via AbortSignal",
    "mockGateway > accepts Money amount input on createPayment",
    "mockGateway > accepts provider_success_client_timeout alias",
    "mockGateway > amount conversion minor units in rawResponse",
    "mockGateway > caches non-throw indeterminate under idempotencyKey",
    "mockGateway > capture scripted money overrides cannot desync ledger dual-write (TESTKIT-1)",
    "mockGateway > capture:false without authorization capability is OperationNotSupportedError (NEW-TESTKIT-4)",
    "mockGateway > concurrent dual-timeout joiners all reject (TESTKIT-2 no false symmetry)",
    "mockGateway > concurrent partial captures serialize and reject over-capture (TESTKIT-1)",
    "mockGateway > concurrent partial captures that fit remaining both settle without over-capture (TESTKIT-1)",
    "mockGateway > concurrent partial refunds serialize and reject over-refund (TESTKIT-1)",
    "mockGateway > concurrent same idempotencyKey does not double-charge",
    "mockGateway > converts capture/refund majors with payment currency (TESTKIT-2)",
    "mockGateway > default create dual-writes outcome succeeded + references",
    "mockGateway > default/succeeded createPayment still writes paid ledger",
    "mockGateway > delayMs alias and FakeClock virtual latency",
    "mockGateway > does not capture after failed create (TESTKIT-1)",
    "mockGateway > does not capture after pending requires_action (TESTKIT-1)",
    "mockGateway > does not capture after voided auth (TESTKIT-1)",
    "mockGateway > economically equivalent Money vs number amount shares fingerprint (TESTKIT-1)",
    "mockGateway > enqueue extends queues at runtime",
    "mockGateway > ensurePaymentLedger clamps scripted captured/refunded to charge total (TESTKIT-4)",
    "mockGateway > failed outcome is definitive success:false status failed",
    "mockGateway > getPayment does not rewrite ledger to paid on outcome succeeded (NEW-TESTKIT-3)",
    "mockGateway > history getter returns a frozen snapshot callers cannot mutate",
    "mockGateway > history redacts error messages that look like secrets",
    "mockGateway > history redacts secrets and assertHistory works",
    "mockGateway > honors idempotencyKey with same gatewayId (process-local)",
    "mockGateway > indeterminate outcome is Phase 6 arm, never decline/paid",
    "mockGateway > maps declines and records history with error summary",
    "mockGateway > non-success scripted capture does not mutate ledger",
    "mockGateway > non-success scripted void does not cancel ledger (ledger integrity)",
    "mockGateway > partial capture and refund with remaining tracking",
    "mockGateway > provider_ok_client_timeout caches so idempotent retry is not a double charge",
    "mockGateway > provider_ok_client_timeout dual outcome records ledger success",
    "mockGateway > provider_ok_client_timeout preserves auth-only provider success (TESTKIT-2)",
    "mockGateway > redacting logger never receives cleartext secrets",
    "mockGateway > refundPayment provider_ok_client_timeout settles ledger then throws (P05-TK-2)",
    "mockGateway > rejects over-capture and over-refund",
    "mockGateway > remaining capturable hold after partial refund stays capturable (NEW-TESTKIT-2)",
    "mockGateway > replays last step after queue exhaustion",
    "mockGateway > requires_action is not type-confusable with paid via isPaidOutcome",
    "mockGateway > requires_action/pending create does not full-capture ledger",
    "mockGateway > same idempotencyKey with different amount is fingerprint_conflict (TESTKIT-1)",
    "mockGateway > same idempotencyKey with different customerId/paymentMethodId/offSession is fingerprint_conflict",
    "mockGateway > same idempotencyKey with different orderId/PM identity is fingerprint_conflict (NEW-TESTKIT-1)",
    "mockGateway > same idempotencyKey with different setup_future_usage/iframe is fingerprint_conflict (NEW-TESTKIT-FP-1)",
    "mockGateway > same idempotencyKey with different stripeCustomer/paymob identity is fingerprint_conflict (NEW-TESTKIT-7)",
    "mockGateway > scripted capture status failed does not settle ledger (TESTKIT-3)",
    "mockGateway > scripted refund result cannot override ledger totals (TESTKIT-3)",
    "mockGateway > scripted/defaultOutcome succeeded + capture:false is authorized (NEW-TESTKIT-6)",
    "mockGateway > scripts outcomes FIFO",
    "mockGateway > throw form steps",
    "mockGateway > timeout and network_error throw NetworkError",
    "mockGateway > timeout does not leave paid ledger",
    "mockGateway > tracks partial capture remaining in integer minor units",
    "mockGateway > uses explicit defaultOutcome when queues empty",
    "mockGateway > void authorized payment",
    "runGatewayConformanceSuite > golden-path mockGateway passes full suite (all cases)",
    "runGatewayConformanceSuite > network_failure fails when result is success:true (P05-CONF-1)",
    "runGatewayConformanceSuite > partial_capture and partial_refund cases pass with strict money asserts (TESTKIT-1)",
    "webhookEventToPaymentEvent > capture dual-write uses partially_completed for partials (CORE-4)",
    "webhookEventToPaymentEvent > maps payment_paid \u2192 payment.succeeded with Payment + provider metadata",
    "webhookEventToPaymentEvent > omits amount without currency on paymentFromWebhookEvent (CORE-3)"
  ],
  "checksPassed": [
    "workspace build",
    "workspace typecheck",
    "public API type tests",
    "workspace boundaries",
    "API/schema compatibility",
    "runtime portability"
  ],
  "unchangedLintFindings": [
    "gateway-myfatoorah/src/gateway.ts interface/class declaration merging (2)",
    "gateway-tap/src/gateway.ts interface/class declaration merging (2)"
  ],
  "providerNetworkCalls": false,
  "notes": [
    "Both full test runs allowed local loopback servers.",
    "Runtime tests use synthetic provider responses, not provider sandbox payments.",
    "A missing local workspace symlink was restored for internal-sql-store; no source dependency changed."
  ]
}
